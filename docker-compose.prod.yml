version: '3.8'

services:
  api:
    environment:
      - APP_ENV=prod
    ports:
      - "127.0.0.1:${VPS_API_PORT:-8001}:8000"
    volumes: []
    restart: always

  admin-app:
    ports:
      - "127.0.0.1:${VPS_ADMIN_PORT:-8002}:3000"
    restart: always

  scraper-renthai:
    volumes:
      - ./scraper-renthai/storage:/app/storage
      - /var/www/amppattaya/media:/media
    environment:
      - MEDIA_ROOT=/media
      - MEDIA_URL_PREFIX=/media
